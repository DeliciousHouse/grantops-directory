# Launch and Credential Status

## Available now

- **GitHub:** authenticated as `DeliciousHouse`; sufficient to publish the code and initial GitHub Pages site.
- **Hermes profile:** `mogul` exists and can run autonomous research/build tasks.
- **Public-source research:** no credential required.

## Owner action required

### 1. Google Workspace OAuth reauthorization

**Why:** partnership outreach, replies, Search Console, Analytics, and revenue reporting by email.

**Current state:** the existing Sugar & Leather OAuth client is present, but its refresh token returns `invalid_grant`; it must be reauthorized. No password or raw OAuth secret should be sent in chat. Sandy can generate the consent URL; Brendan completes Google consent and returns only the final redirect URL.

### 2. Instrumentl partner application and payout onboarding

**Why:** the first verified revenue path is Instrumentl's partner program.

**Application information:** owner-approved name, Sugar & Leather company name, business email, and the launched site URL. After acceptance and a qualified referral, Instrumentl states that payout is handled through Gusto.

**Owner-only information:** legal/tax identity and payout bank details must be entered directly into the vendor's secure onboarding flow. Do not send them to Sandy in chat.

## Optional after validation

### Custom domain and DNS

The first version can launch free on GitHub Pages. A custom domain later requires either delegated Cloudflare/registrar access or an owner-completed DNS change. Buying a domain is not authorized by the zero-spend launch and needs a spending decision.

### Analytics

Search Console and Google Analytics can use the same Google reauthorization. The site can launch without them; they are needed before declaring organic growth or conversion performance.

## Not needed

- No X/Twitter credential is needed to implement the playbook.
- No payment processor is needed for affiliate revenue.
- No new hosting credential is needed for the initial GitHub Pages launch.
